"""Asset bundle processing: download, extraction, media decoding, and paths.

``pipeline`` orchestrates the per-bundle work; the sibling modules hold the
format-specific pieces (audio, video, images, paths, integrity, runtime pools).
"""
